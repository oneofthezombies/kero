{
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "commander": "12.0.0",
    "husky": "9.0.11",
    "sort-package-json": "2.8.0"
  }
}
